import SwiftUI

struct LoginView : View {
    var body : some View {
        VStack {
            Image("home icon")
        }
        .background(.customDarkBlue)
    }
}

#Preview {
    LoginView()
}
